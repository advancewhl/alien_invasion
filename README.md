# Alien Invasion

一个用 **Python + Pygame** 打造的小游戏练习，目前实现了窗口初始化和飞船渲染，结构清晰，便于继续拓展。

- `alien_invasion.py`：入口与主循环  
- `setting.py`：屏幕尺寸、颜色等配置  
- `ship.py`：飞船类（加载、缩放、绘制）  
- `images/`：素材文件，如 `ship.bmp`

运行前确保安装 pygame：

```bash
pip install pygame
python alien_invasion.py
```